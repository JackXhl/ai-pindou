# 站点绝对 URL，作为 canonical、hreflang、sitemap、OG 图的基准。
# 全项目禁止手写域名字符串，域名只在 src/lib/url.ts 中出现一次并从这里读取。
# 生产示例：
#   Cloudflare Workers: PUBLIC_SITE_URL=https://ai-pindou.aipindou.workers.dev
#   自有域名:           PUBLIC_SITE_URL=https://pindou.example.com
PUBLIC_SITE_URL=http://localhost:4321

# 子路径前缀：本地开发与子路径部署可共用同一套。
# 本地访问：http://localhost:4321/aipindou/
# 若挂在域名根（如 Cloudflare workers.dev），构建时覆盖：
#   PUBLIC_BASE_PATH=/ pnpm build
PUBLIC_BASE_PATH=/aipindou

# 意见反馈链接（页眉「意见反馈」按钮）。留空则使用 GitHub Issues 默认地址。
PUBLIC_FEEDBACK_URL=
